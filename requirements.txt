# Minimal deps for app.py (Streamlit + OpenAI only).
# Use Python 3.12 on Streamlit Community Cloud (Advanced settings) — avoid 3.14
# with old pins; pandas/plotly were removed (not used in this app).
streamlit>=1.40.0,<2
openai>=1.50.0
